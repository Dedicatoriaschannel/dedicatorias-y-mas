---
import Layout from '../../layouts/Layout.astro';
import VideoGrid from '../../components/VideoGrid.astro';
import { CATEGORIES } from '../../data/categories';
import { getVideosByCategory } from '../../data/mockVideos';

export async function getStaticPaths() {
  return CATEGORIES.map(category => ({
    params: { slug: category.slug },
    props: { category },
  }));
}

const { category } = Astro.props;
const videos = getVideosByCategory(category.slug);

// Metadata per SEO
const title = `${category.title} | Dedicatorias y Más`;
const description = category.description;
---

<Layout title={title} description={description}>
  <section class="relative pt-32 pb-20 overflow-hidden min-h-[50vh] flex items-center justify-center">
    <div class="absolute inset-0 bg-brand-black"></div>
    <div class="absolute inset-0 bg-gradient-to-b from-brand-gold/10 via-transparent to-brand-black/90"></div>
    
    <div 
      class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[20rem] h-[20rem] md:w-[30rem] md:h-[30rem] opacity-[0.03] pointer-events-none select-none blur-sm animate-pulse text-white"
      set:html={category.icon}
    >
    </div>

    <div class="absolute top-1/4 left-1/4 w-32 h-32 bg-brand-gold/20 rounded-full blur-[80px] pointer-events-none"></div>
    <div class="absolute bottom-1/4 right-1/4 w-40 h-40 bg-brand-gold/10 rounded-full blur-[100px] pointer-events-none"></div>

    <div class="container mx-auto px-4 relative z-10 text-center">
      <div class="animate-fade-in">
        <span class="inline-block px-4 py-1.5 mb-6 border border-brand-gold/30 rounded-full bg-brand-gold/10 text-brand-gold text-sm font-medium backdrop-blur-sm uppercase tracking-wider">
          Categoría
        </span>
        
        <h1 class="font-display text-5xl md:text-7xl font-bold text-white mb-6 drop-shadow-lg">
          {category.title}
        </h1>
        
        <p class="text-xl md:text-2xl text-white/70 max-w-2xl mx-auto font-light leading-relaxed font-['Great_Vibes'] text-brand-gold-light">
          {category.description}
        </p>
      </div>
    </div>
  </section>

  <div class="bg-brand-black min-h